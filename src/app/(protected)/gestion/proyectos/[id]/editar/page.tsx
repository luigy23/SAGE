import DetalleProyectoPage, { metadata as originalMetadata } from "@/app/(protected)/proyectos/[id]/page"

export const metadata = {
  ...originalMetadata,
  title: "Editar Proyecto | Gestión SAGE"
}

export default DetalleProyectoPage
